/**
 * @brief name space containing enum of return codes for program
 * 
 */

namespace returncodes {
    enum returncodes
    {
        SUCCESS,
        IMPROPER_USAGE,
        FILE_READ_ERROR,
    };
};
